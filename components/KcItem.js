import React, { Component } from 'react'
import Router from 'next/router';

export default class KcItem extends Component {
    constructor(props) {
        super(props);
    }
    render () {
        const { data } = this.props;
        return (
            <div></div>
        )
    }
}
